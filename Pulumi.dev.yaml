apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: secret-store
spec:
  provider:
    pulumi:
      organization: swati796
      project: crd-bug-repro
      environment: dev
      accessToken:
        secretRef:
          key: apiKey
